{
  "source": "none",
  "note": "Intentionally EMPTY — the committed snapshot applies NO correction at runtime (loadCorrectionMap returns an empty map). HONESTY GUARDRAIL: a correction built from one model's gold eval must not silently nudge the live elicitor's conditionals. Training snapshots are MEASUREMENTS only — do not commit them populated. Latest measurements: DeepSeek (1052) sign 82.0%, reliability 86%; qwen3-max (1052) sign 92.0%, reliability 100% — qwen3-max is accurate enough that the correction barely helps (LOO pFail 0.140->0.135), so it stays off. See test/relation-correction-wiring.test.ts.",
  "byMechanismType": {}
}
